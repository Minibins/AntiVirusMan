using UnityEditor;
using UnityEngine;

public class SettingsRedactor : Editor
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
